# Use an official Node.js runtime as a parent image
FROM node:20-alpine

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy package files first for better caching
COPY package*.json ./

# Install dependencies (skip postinstall since dashboard files aren't copied yet)
RUN npm ci --ignore-scripts

# Install the Vendure CLI globally in the container
RUN npm install -g @vendure/cli

# Copy the rest of the application code
COPY . .

# Build the dashboard (now that all files are present)
RUN npx vite build

# Make port 3001 available to the world outside this container
EXPOSE 3001

# Run the command to start the app in dev mode
CMD ["npm", "run", "dev"]